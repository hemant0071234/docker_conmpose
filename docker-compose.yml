postgresnew:
  build: .
  expose:
    - "5432"
  volumes:
    - /etc:/postgre/etc

calling2:
  build: ./Caller
  links: 
    - 'postgresnew:calling'
  volumes:
     - ./code:/code

  volumes_from:
    - postgresnew